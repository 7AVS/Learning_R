This is slide 2 of a two-slide executive deck on CRV cannibalizing PCL on the mobile banner (MB) channel at RBC. Slide 1 established the top-line finding: MB audience is saturated, PCL cannibalization is confirmed, and approximately $30MM NIBT is recoverable if CRV stops running against overlapped clients. Slide 2 explains the mechanism — what happens to clicks when CRV runs alongside PCL — and the economics — the net conversion and NIBT balance on overlapped clients. All figures are computed on the overlapped population only: clients eligible for both CRV and PCL on the mobile banner channel.

Headline to render exactly: "CRV trades even on clicks but loses 2:1 on conversions — a net −$31.5MM NIBT swap on overlapped clients"

Kicker text (grey, top-left, uppercase, small): "Campaign Performance: Credit Card Installment Trigger [CRV]"

Match slide 1's visual template exactly: same navy headline style (~#1a4e8c or close dark navy, bold, ~24px), same grey uppercase kicker, same two-column panel layout, teal-outlined callout boxes (#00B2A9 or close), bullets below each panel, clean sans-serif font (Segoe UI or Arial).

---

LEFT PANEL — "Where clicks go when CRV runs alongside PCL"
Panel subtitle: [PERIOD] | Overlapped clients, MB channel

Visual: a grouped bar chart with two groups side by side — "PCL clicks" and "CRV clicks" — each group showing two bars: Control (no CRV shown) and Action (CRV shown alongside PCL).

Bar values:
- PCL clicks: Control = 14%, Action = 10%
- CRV clicks: Control = 0%, Action = 5%

Color convention: Control bars in navy (#1a4e8c or match slide 1 blue), Action bars in a muted orange or medium grey to distinguish. Value labels on top of each bar.

Teal callout box on the chart: "PCL clicks −29% when co-exposed"

Two bullets below the panel:
1. Attention is split, not lost — among clients who view both banners, click-through is ~32% vs. 35% for PCL alone; PCL's share of clicks drops from 35% to 26%.
2. CRV rarely wins outright — only 1% of overlapped clients click CRV exclusively; 12% click both banners.

---

RIGHT PANEL — "Net balance of running CRV on overlapped clients"
Panel subtitle: [PERIOD] | Overlapped clients, MB channel

Visual: a clean table with the following rows and columns. Render negative values in muted red, positive values in standard black or dark grey. Do not add any numbers not listed here.

Table columns: Outcome | With CRV (Action) | Without CRV (Control) | Gap | Per 1,000 overlapped | Total conversions | NIBT

Row 1 — PCL conversion: 16.63% | 17.75% | −1.12pp | −11 | −46,967 | −$31.7MM
Row 2 — CRV conversion: 1.99% | 1.53% | +0.46pp | +5 | +19,106 | +$0.2MM

Net NIBT row (bold, below table): Net NIBT = −$31.5MM

Green callout box below the table: "Running CRV buys 5 CRV conversions but costs 11 PCL conversions per 1,000 overlapped clients"

Two bullets below the panel:
1. The value asymmetry drives the net: ~$675 NIBT per PCL conversion vs. ~$11 per CRV conversion — a ~60× gap.
2. Net of CRV's own gains: −$31.5MM NIBT on overlapped clients.

---

Format and style rules:
- Use 2-decimal percentages where the source data has two decimals (16.63%, 17.75%, 1.99%, 1.53%). Use whole numbers where source is whole (14%, 10%, 0%, 5%).
- No hedging language anywhere — do not write "directional", "under review", "pending", "being validated", or any equivalent.
- Keep visuals simple — executives read this in 30 seconds. No decorative elements.
- [PERIOD] is a placeholder Andre will replace to match slide 1's date-range label. Do not guess or fill in a date.
- The two-column layout must be visually balanced. Table in the right panel should be compact but readable.
- No emoji, no marketing language, no "key takeaway" section headers.
